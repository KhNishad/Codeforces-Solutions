#include<bits/stdc++.h>
using namespace std;
int main() {

 int a , b, i=0,sum=0;
 int lop = true;
 cin>>a>>b;
 while(lop){
     i++;
     sum = a*i;

     if(sum%10 == 0 || sum%10 == b)
     {
         cout<<i<<endl;
         lop = false;
     }
 }
}
